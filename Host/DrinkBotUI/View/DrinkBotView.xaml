<Window x:Class="DrinkBotUI.View.DrinkBotView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DrinkBotUI.ViewModel"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        
        d:DataContext="{d:DesignInstance Type=vm:DrinkBotViewModel, IsDesignTimeCreatable=True}"
        
        Title="DrinkBot UI" Height="435.821" Width="541.791"
        
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="29*"/>
            <ColumnDefinition Width="149*"/>
            <ColumnDefinition Width="178*"/>
            <ColumnDefinition Width="178*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" Grid.ColumnSpan="2"/>
        <ListBox Grid.Column="2" ItemsSource="{Binding Recipes}" SelectedItem="{Binding SelectedRecipe}"/>
        <Button Content="Make Drink" Grid.Column="3" Command="{Binding DispenseCommand}" IsEnabled="{Binding ReadyToDispense}"/>
        <Label Grid.Row="1" Content="{Binding DatabaseState}" Grid.ColumnSpan="2" />
    </Grid>
</Window>
