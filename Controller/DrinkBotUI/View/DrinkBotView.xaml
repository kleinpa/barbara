<Window x:Class="DrinkBotUI.View.DrinkBotView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DrinkBotUI.ViewModel"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        
        d:DataContext="{d:DesignInstance Type=vm:DrinkBotViewModel, IsDesignTimeCreatable=True}"
        
        Title="DrinkBotView" Height="435.821" Width="541.791"
        
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"/>
        <ListBox Grid.Column="1" ItemsSource="{Binding Recipes}" SelectedItem="{Binding SelectedRecipe}"/>
        <Button Content="Make Drink" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Width="75" Command="{Binding DispenseCommand}"/>
        <Label Grid.Row="1" Content="{Binding DatabaseState}" />
    </Grid>
</Window>
